name: Rust

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: recursive
      - name: Stable with rustfmt and clippy
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          components: rustfmt, clippy

      - name: Annotate commit with clippy warnings
        uses: actions-rs/clippy-check@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          args: --all-features

      - name: Build SDL2 dependency
        run: |
          export PREFIX=`pwd`
          mkdir lib && mkdir include && mkdir bin && cd SDL2 && ./configure --prefix=$PREFIX && make -j4 && make install

      - name: Build SDL2_ttf dependency
        run: |
          export PREFIX=`pwd`
          export SDL2_DIR=`pwd`/SDL2
          cd SDL2_ttf && ./configure --prefix=$PREFIX && make -j4 && make install

      - name: Build
        run: cargo build --verbose
      - name: Run tests
        run: cargo test --verbose
      - name: Run doc
        run: cargo doc --no-deps

      - uses: actions/upload-artifact@v2
        with:
          name: doc-artifact
          path: target/doc

  doc:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' || github.event.pull_request.merged == true

    steps:
      - uses: actions/checkout@v2
        with:
          submodules: recursive
      - uses: actions/download-artifact@v2
        with:
          name: doc-artifact

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./
